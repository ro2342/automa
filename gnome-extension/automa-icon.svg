<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" width="64" height="64">
  <!-- Fundo circular -->
  <circle cx="32" cy="32" r="30" fill="none" stroke="currentColor" stroke-width="4"/>
  <!-- Letra A estilizada (Automa) -->
  <path d="M18 48 L32 16 L46 48" 
        fill="none" stroke="currentColor" stroke-width="5" 
        stroke-linecap="round" stroke-linejoin="round"/>
  <!-- Barra do A -->
  <line x1="22" y1="36" x2="42" y2="36" 
        stroke="currentColor" stroke-width="5" 
        stroke-linecap="round"/>
  <!-- Ponto de status (pequeno círculo no canto inferior direito) -->
  <circle cx="50" cy="50" r="8" fill="currentColor" class="status-dot"/>
</svg>
